import * as monthAmount from './month.amount'
import * as monthNum from './month.num'
import * as quarterAmount from './quarter.amount'
import * as quarterNum from './quarter.num'

export default {
  ...monthAmount,
  ...monthNum,
  ...quarterAmount,
  ...quarterNum,
}